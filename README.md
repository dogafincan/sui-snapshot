# Sui Holders Snapshot

TanStack Start web app for running Sui holder snapshots on Cloudflare Workers.

The app:

- requests indexed holder balances from Blockberry in Worker-safe page batches,
- renders the full result set in a TanStack Table UI,
- exports the same rows as CSV without rerunning the snapshot.

## Toolchain

This repo now uses Vite+ as the primary local toolchain while keeping the same
TanStack Start and Cloudflare Workers architecture.

Primary workflow:

- install `vp`
- run `vp env setup`
- run `vp install`
- use `vp dev`, `vp check`, `vp test`, and `vp build`
- use `vp run deploy` and `vp run cf-typegen` for Wrangler-specific repo tasks

## What It Does

Input:

- Sui coin type in `0xPACKAGE::MODULE::TOKEN` format

Output:

- ranked holder table
- client-side CSV download with exactly `rank,address,balance`

## Stack

- TanStack Start
- Cloudflare Workers via `@cloudflare/vite-plugin`
- shadcn/ui on Base UI primitives
- preset `b1aIaos55` on `base-luma`
- Tailwind CSS v4
- Inter variable font
- Hugeicons for preset-managed UI icons
- TanStack Table
- Vite+

## Project Structure

- `src/routes/index.tsx`: route entrypoint for `/`
- `src/routes/__root.tsx`: root document and global app shell
- `src/components/snapshot-workbench.tsx`: page layout, form, loading states, summary cards, and results card
- `src/components/holders-table.tsx`: TanStack Table configuration, filtering, sorting, and pagination
- `src/components/ui/field.tsx`: shadcn field composition used for form layout
- `src/lib/sui-snapshot.server.ts`: Blockberry holder page-batch execution
- `src/lib/sui-snapshot.functions.ts`: TanStack Start server function wrapper
- `src/lib/sui-snapshot.ts`: shared validation, formatting, and CSV helpers
- `wrangler.jsonc`: Cloudflare Worker configuration

Generated files:

- `src/routeTree.gen.ts`
- `worker-configuration.d.ts`

These are generated by the framework/tooling and should not be treated as normal hand-edited source files.

## Run Locally

1. Install Vite+ and set up its environment:

   ```bash
   curl -fsSL https://vite.plus | bash
   vp env setup
   ```

2. Install dependencies:

   ```bash
   vp install
   ```

3. Create `.dev.vars` with your local Blockberry secret:

   ```bash
   BLOCKBERRY_API_KEY="your-api-key"
   ```

4. Start the local dev server:

   ```bash
   vp dev
   ```

5. Open [http://localhost:3000](http://localhost:3000).

Production-style local preview:

```bash
vp preview --host 127.0.0.1
```

That serves the built app on `http://127.0.0.1:4173`.

The app requires `BLOCKBERRY_API_KEY` as a server-only Worker secret. Keep local
values in `.dev.vars`, and set the deployed Worker secret with Wrangler or the
Cloudflare dashboard before deploying.

## Scripts

- `vp dev` starts the local TanStack Start dev server on port `3000`.
- `vp check` runs formatting, linting, and type checking through Vite+.
- `vp test` runs the test suite through Vite+.
- `vp build` builds client and server bundles for Workers.
- `vp preview` serves the production bundle locally.
- `vp run deploy` builds and deploys with Wrangler.
- `vp run cf-typegen` regenerates `worker-configuration.d.ts` from `wrangler.jsonc`.

Compatibility wrappers still exist in `package.json` for npm-based workflows:

- `npm run dev`
- `npm run check`
- `npm run test`
- `npm run build`
- `npm run preview`
- `npm run deploy`
- `npm run cf-typegen`

## Deployment

Deploy to Cloudflare Workers with:

```bash
vp run deploy
```

The Worker entrypoint is `@tanstack/react-start/server-entry`, configured in
`wrangler.jsonc`.

Before deploying the Blockberry-backed app, set the required Worker secret:

```bash
npx wrangler secret put BLOCKBERRY_API_KEY
```

## Verification

Useful checks before deploying:

```bash
vp check
vp test
vp build
vp run cf-typegen
```

## Notes

- The app is stateless and public by design. No D1, KV, R2, or background jobs.
- The exported CSV contract is intentionally fixed to `rank,address,balance` so
  it can be uploaded directly into the sibling `sui-airdrop` app.
- Vite+ is the primary toolchain. `vite.config.ts` is the source of truth for
  format, lint, test, and build configuration.
- Generated files such as `src/routeTree.gen.ts` and `worker-configuration.d.ts`
  are excluded from Vite+ formatting and linting.
- The UI is intentionally constrained to stock shadcn `base-luma` styling with the
  applied preset. Prefer stock shadcn components and minimal layout classes.
  Avoid custom page chrome, decorative shells, or bespoke visual treatments.
- Snapshot accuracy follows Blockberry's indexed holder data for the requested
  coin type.
- Large holder sets are fetched across multiple server calls so each Worker
  invocation stays below the Workers Free subrequest limit.
- The original CLI script has been removed; this repository is now web-app-first.
